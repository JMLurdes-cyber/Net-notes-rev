# Overview
One of the error counters that rises whenever an [[Ethernet]] [[Data Link Frames|Frame]] is less than 64 bytes in total length

# Core Ideas
- Often caused by a collision (rare in modern networks, but not impossible to see).
